<%= form_for Comment.new do |f| %>
  <% if Comment.new.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(Comment.new.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% Comment.new.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
 <div class="new_form ">
	 <div class="field" data-no-turbolink>
	    <%= f.label :content %><br>
	    <%= f.text_area :content, class: 'form-control' %>
	    <%= hidden_field_tag(:commentable_type, "Post") %>
	    <%= hidden_field_tag(:commentable_id, @post.id) %>
	    <br />
	 </div>
	  
	 <div class="actions">
	    <%= f.submit "Submit", class:'btn btn-primary' %>
	  </div>
  </div>
  <% end %>