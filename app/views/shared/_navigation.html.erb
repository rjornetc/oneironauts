<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only"><i class="fa fa-burguer"></i></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/"><%= image_tag "logo_xxs.png", style:'display:inline;', alt:'Oneironauts' %></a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <li><a href="/"><i class="fa fa-home"></i> Home</a></li>
        <li><%= link_to posts_path do %><i class="fa fa-newspaper-o"></i> Blog <% end %></li>
        <li><%= link_to dream_signs_path do%><i class="fa fa-book"></i> Dream Dictionary<%end%></li>
        <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><i class="fa fa-paper-plane-o"></i> Guides <span class="caret"></span></a>
                  <ul class="dropdown-menu" role="menu">
                    <li><%= link_to "Sleep Habits", sleep_guide_path %></li>
                    <li class="divider"></li>
                  </ul>
                </li>
        <% if current_user %>
            <li><%= link_to groups_path do%><i class="fa fa-users"></i> Groups<%end%></li>
            <li><%= link_to user_sleep_logs_path(current_user.id) do%><i class="fa fa-bar-chart"></i> Sleep Log<%end%></li>
            <li><%= link_to user_profile_path(current_user) do %>
                <% if current_user.avatar.exists? %>
                    <%= image_tag current_user.avatar.url(:medium), class: 'mini-avatar' %>
                <% else %>
                    <%= image_tag current_user.gravatar_url(default: 'identicon'), class: 'mini-avatar' %>
                <% end %>
             Me <% end %></li>
            <li><%= link_to destroy_user_session_path, method: :delete, :class => 'navbar-link' do%><i class="fa fa-sign-out"></i> Log out<%end%></li>
        <% else %>
            <li><%= link_to new_user_session_path do %><i class="fa fa-sign-in"></i> Log in<% end %></li>
            <li><%= link_to new_user_registration_path do %><i class="fa fa-user-plus"></i> Sign up<% end %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>